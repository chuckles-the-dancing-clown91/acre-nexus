# Rust
/backend/target/
**/target/

# Node / Next.js
/frontend/node_modules/
**/node_modules/
/frontend/.next/
**/.next/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env*.local
!.env.example
!.env.local.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local object store (document service dev backend). The STORAGE_DIR default is
# relative (`./data/objects`), so it lands under whichever crate dir the process
# runs from — cover both the repo root and the `api` crate (server/tests).
/backend/data/
/backend/crates/api/data/
**/data/objects/
