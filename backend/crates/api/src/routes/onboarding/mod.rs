//! Property **onboarding** — the full intake flow that brings a new house onto
//! the platform (property + financing + workflow start + enrichment). Mounted by
//! the `properties` module.

pub mod dto;
pub mod onboard;
pub mod state;
pub mod workflow;
